(function(){var canvas=document.getElementById('stage'),ctx=canvas.getContext('2d'),cam={x:innerWidth*.42,y:170,zoom:1},drag=null;window.App={labels:true};function resize(){var d=devicePixelRatio||1;canvas.width=innerWidth*d;canvas.height=innerHeight*d;canvas.style.width=innerWidth+'px';canvas.style.height=innerHeight+'px';ctx.setTransform(d,0,0,d,0,0);cam.x=Math.min(innerWidth*.43,520)}resize();addEventListener('resize',resize);canvas.addEventListener('pointerdown',function(e){drag={x:e.clientX,y:e.clientY,cx:cam.x,cy:cam.y};canvas.setPointerCapture(e.pointerId)});canvas.addEventListener('pointermove',function(e){if(drag){cam.x=drag.cx+e.clientX-drag.x;cam.y=drag.cy+e.clientY-drag.y}});canvas.addEventListener('pointerup',function(){drag=null});canvas.addEventListener('wheel',function(e){e.preventDefault();cam.zoom=Math.max(.45,Math.min(1.7,cam.zoom*(e.deltaY>0?.9:1.1)))},{passive:false});UI.init();document.getElementById('btn-run').onclick=function(){UI.resetTutorial()};document.getElementById('btn-play').onclick=function(){Sim.paused=!Sim.paused};document.getElementById('btn-step').onclick=function(){if(!Sim.needsInput)Sim.advance()};document.getElementById('btn-reset').onclick=function(){UI.resetTutorial();document.getElementById('editor-sec').hidden=true};document.getElementById('labels').onchange=function(e){App.labels=e.target.checked};var last=performance.now();function frame(now){var dt=Math.min(.05,(now-last)/1000);last=now;var sp=parseFloat(document.getElementById('speed').value);document.getElementById('v-speed').textContent=sp.toFixed(1)+'×';Sim.update(dt,sp);ctx.clearRect(0,0,innerWidth,innerHeight);Render.draw(ctx,cam);UI.update();requestAnimationFrame(frame)}requestAnimationFrame(frame)})();